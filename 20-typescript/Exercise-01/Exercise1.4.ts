

const firstName = "John";           // Type: string
let counter = 0;                    // Type: number
const PI = 3.14159;                 // Type: number
let prices = [10.99, 20.50, 5.25];  // Type: number[]
const isValid = true;               // Type: boolean

function getFullName(first: string, last: string) {
    return first + " " + last;      // Return type: string
}