var firstName = "John"; // Type: string
var counter = 0; // Type: number
var PI = 3.14159; // Type: number
var prices = [10.99, 20.50, 5.25]; // Type: number[]
var isValid = true; // Type: boolean
function getFullName(first, last) {
    return first + " " + last; // Return type: string
}
